import React from 'react';
import './Presets.css';

class Presets extends React.Component {

  render() {
    return (
      <div className="presets">
        <div className="preset fill"></div>
      </div>
    )
  }
}

export default Presets;
